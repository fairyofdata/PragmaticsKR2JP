# PragmaticsKR2JP — 한국어 화자를 위한 개인 일본어 작문 코치

## 사용자
- 한국어 모어 화자, 일본(오카야마)에서 일하는 엔지니어. JLPT N2, 업무는 일본어.
- 약점: 문법 정확성, 맥락·뉘앙스에 맞는 어휘·표현 선택, 속도.
- LLM 페어프로그래밍으로 만든다. 코드는 읽을 수 있지만 쓰는 건 약하다
  → 코드는 단순하게, 새 개념은 짧게 설명, **큰 설계 결정은 먼저 물어본다**.
- 대화는 한국어.

## 설계 원칙 (가장 중요)

### 1. LLM은 주석자(annotator)다. 측정과 판정의 나머지는 코드가 한다.
| LLM이 하는 일 | LLM에게 맡기지 않는 일 | 코드가 하는 일 |
|---|---|---|
| 과제 출제, 고정 유형표 안에서 오류 위치·유형 태깅, 교정문, 한국어 해설 | 점수, 수준 판정, 누적·순위·추세, 자기 결과의 검증 | 인용 검증, 미태깅 수정 검출, 다수결, 집계·순위, 신뢰도(일치율)·타당도(탐지율) 측정 |

- 항목 단위의 오류 판단 자체는 LLM이 하는 측정이다. 따라서 "결정론적으로 집계했으니 객관적"이 아니다.
  주석의 신뢰도(재현성 실험)와 타당도(탐지율 실험)를 측정하고, 버전별로 공개한다.
- 버전(taxonomy / prompt / schema / model)이 다른 기록은 섞어서 집계하지 않는다.

### 2. 끌려가지 않기 (기술적 핵심)
틀린 입력을 "아마 이런 뜻이겠지" 하고 맞는 쪽으로 읽어버리는 경로를 모두 막는다.
- A. 입력 원문을 정규화 없이 그대로 저장.
- B. 해석(intended_meaning_ko)과 판정(errors)을 분리.
- C. minimal_correction(오류만) / natural_version(장면에 맞는 문장)을 분리.
- D. 태그의 original 이 원문에 글자 그대로 있는지 코드가 검증 (`coach/verify.py`, `coach/voting.py`).
- E. 원문 → minimal_correction 의 바뀐 글자가 모두 태그로 덮였는지 코드가 검증 (미태깅 수정은 화면·기록에 드러냄).
- E2. 태그 하나에 다른 단어의 수정이 합쳐졌는지 코드가 검출 (`を合→に会`: 조사 태그 하나에 한자 변환 실수가 묻힘). 좁은 규칙이라 놓치는 경우 있음.
- 전각/반각 차이만 있는 수정은 오류가 아니다 (비교할 때만 NFKC 사용, 저장은 원문 그대로).
- F. 오류를 심은 평가 세트로 탐지율 측정 (`experiments/`).
- 교훈: 프롬프트 규칙(태그 분리, 반각 허용)은 모델이 안정적으로 따르지 않았다. 지킬 수 있는 규칙은 코드로 옮긴다.
- 반대 방향 실패(과교정)도 막는다: "오류 없음"이 정상 결과, 무오류 답에 붙는 태그 수를 측정.
- 이 부분은 계속 점진적으로 강화한다.

### 3. 프롬프트
- 답 전체를 보여주는 고정 few-shot 은 넣지 않는다 (주제·유형 쏠림). 유형마다 과제와 무관한 짧은 대비쌍 1개만, 모든 유형에 균등하게.
- 흔들림은 temperature 가 아니라 **n회(기본 3) 채점 후 수정 단위 다수결**로 다룬다. Gemini 3 는 temperature 1.0 유지가 공식 권장.
- 프롬프트를 바꾸면 `PROMPT_VERSION`, 유형표를 바꾸면 `TAXONOMY_VERSION`, 기록 형식을 바꾸면 `SCHEMA_VERSION` 을 올린다.
- 평가 세트 문장과 프롬프트의 대비쌍은 겹치지 않게 한다 (누수 방지).

### 4. 모드 분리
- 문법 모드(형태·통사: 정확성) / 표현 모드(어휘·화용: 적절성). 모드마다 유형 수를 줄여 재현성을 높인다.
- 모드 밖 오류는 `OUT_OF_MODE` 로만 태깅 (검증 E 유지용, 집계 제외).
- 유형표: `coach/taxonomy.py` (현재 v1). 순서 = 우선순위, 더 구체적인 유형이 위.

### LLM 제공자
- 기본 OpenAI (`gpt-5.6-terra` 채점, `gpt-5.6-luna` 출제). `LLM_PROVIDER=gemini` 로 전환 가능.
- Gemini 무료 등급은 하루 20회 한도라 실험을 못 돌렸다 (2026-09-20).

### 5. 동기부여
- 연속 사용일 등은 제품 기능으로 둘 수 있지만 **기본 꺼짐**. 하루 할당량·점수 목표를 강제하지 않는다.

## 구조
- `app.py` Streamlit 화면 (연습 / 요약)
- `coach/llm.py` LLM 호출은 여기에만 (제공자 교체 지점)
- `coach/verify.py`, `coach/voting.py`, `coach/stats.py` LLM 없는 순수 코드. `tests/` 로 검사
- `experiments/` 재현성·탐지율 실험, 평가 세트, 결과
- `data/` 실제 기록 (gitignore, append-only) / `samples/` 시연용 합성 예시 (커밋)

## 범위
- 이번(MVP) 범위: 텍스트 입력, 한 바퀴(출제→작성→채점→저장→상위 5개 요약), 재현성 실험.
- 범위 밖 (README "다음 단계"에만): 음성 입력, 정기 시험, 복습 스케줄러, 차트 여러 개, 로그인, 배포.

## 개인정보
- 이 프로젝트 폴더 밖은 읽지 않는다. 특히 `C:\Users\fairy\Self Analysis\` 는 절대 열지 않는다.
- 예외: `C:\Users\fairy\PhonemeJP2KR` 는 필요할 때 참고만 (방향성이 다르다).
- 다루는 데이터는 사용자가 이 앱에 직접 쓴 일본어 답뿐.

## 명령
- 실행: `.venv\Scripts\streamlit run app.py`
- 테스트: `.venv\Scripts\python -m pytest -q`
- 실험: `.venv\Scripts\python -m experiments.repro` / 시연 예시: `.venv\Scripts\python -m experiments.build_samples`
