# 이 파일을 .env 로 복사하고 키를 넣는다. .env 는 git 에 올라가지 않는다.
# 쓰는 제공자의 키 하나만 있으면 된다.
OPENAI_API_KEY=
GEMINI_API_KEY=
# 아래는 선택 (기본값)
# LLM_PROVIDER=openai
# GRADER_MODEL=gpt-5.6-terra
# TASK_MODEL=gpt-5.6-luna
# GRADER_REASONING=low
# N_SAMPLES=3
